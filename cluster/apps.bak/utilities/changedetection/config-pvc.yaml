apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: changedetection-data-volume
spec:
    accessModes:
        - ReadWriteOnce
    storageClassName: longhorn
    resources:
        requests:
            storage: 256Mi

