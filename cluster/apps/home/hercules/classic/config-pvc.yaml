apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: hercules-classic-volume
    labels:
      app: hercules-classic
spec:
    accessModes:
        - ReadWriteOnce
    storageClassName: longhorn
    resources:
        requests:
            storage: 3Gi

