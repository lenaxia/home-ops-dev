apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: timescaledb-config-volume
spec:
    accessModes:
        - ReadWriteOnce
    storageClassName: longhorn
    resources:
        requests:
            storage: 1Gi

