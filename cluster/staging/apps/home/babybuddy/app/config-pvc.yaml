apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: babybuddy-volume
    labels:
      app: babybuddy
spec:
    accessModes:
        - ReadWriteOnce
    storageClassName: longhorn
    resources:
        requests:
            storage: 256Mi

