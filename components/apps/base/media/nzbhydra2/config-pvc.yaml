apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: nzbhydra2-config-volume
    podLabels:
      app: nzbhydra
spec:
    accessModes:
        - ReadWriteOnce
    storageClassName: longhorn
    resources:
        requests:
            storage: 64Mi

