apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: guacamole-config-volume
spec:
    accessModes:
        - ReadWriteOnce
    storageClassName: longhorn
    resources:
        requests:
            storage: 512Mi

