apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: guacamole-config-volume
    namespace: utilities
spec:
    accessModes:
        - ReadWriteOnce
    storageClassName: longhorn
    resources:
        requests:
            storage: 512Mi

