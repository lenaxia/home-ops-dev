apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: librespeed-config-volume
spec:
    accessModes:
        - ReadWriteOnce
    storageClassName: longhorn
    resources:
        requests:
            storage: 256Mi

